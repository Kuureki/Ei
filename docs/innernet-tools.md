# innernet MCP tool manifest

Discovered at runtime from an authenticated innernet connection
(`eve info`, see plan task 2 step 4). Fill the two lines below with the
real tool names; `scripts/check-innernet-manifest.sh` fails until they are.

- `SAVE_TOOL = <innernet__...>`: saves a memory entry (content + metadata)
- `SEARCH_TOOL = <innernet__...>`: ranked search over memory
