# innernet MCP tool manifest

Discovered at runtime from an authenticated innernet connection
(`eve info`, plan task 2 step 4). Verified live against innernet 2.0.0
(2026-08-15); the MCP server exposes 35 tools under the `innernet__` prefix.

SAVE_TOOL = innernet__innernet_capture
SEARCH_TOOL = innernet__innernet_context